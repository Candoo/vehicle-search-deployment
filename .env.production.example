# Production Environment Variables
#
# NOTE: For production deployments, copy this file and update with real values:
#   cp .env.production.example .env.production
#
# IMPORTANT: Never commit .env.production with real credentials!

# Database Configuration - Update with your production database
DB_HOST=your-production-db-host
DB_PORT=5432
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your-db-name
DB_SSLMODE=require

# API Configuration
API_PORT=8080
GIN_MODE=release

# Frontend Configuration - Update with your production API URL
NUXT_PUBLIC_API_BASE=https://api.yourdomain.com
FRONTEND_PORT=3000